<UserControl x:Class="WindowsCalculator.ProgrammerCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WindowsCalculator"
             mc:Ignorable="d"
             Height="471" Width="321">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Display Area -->
        <Grid Grid.Row="0" Background="#202020">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="20" />
                <RowDefinition Height="70" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="40">
                <TextBlock x:Name="TitleTextBlock"
                           Text="Programmer"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="15,0,0,0" />
            </StackPanel>

            <TextBlock Grid.Row="1" x:Name="ExpressionTextBlock"
                       Text="{Binding ExpressionText}"
                       Foreground="#BFD6D6D6"
                       FontSize="14"
                       TextAlignment="Right"
                       Margin="10,0,15,0" />

            <TextBlock Grid.Row="2" x:Name="ResultTextBlock"
                       Text="{Binding DisplayText}"
                       Foreground="White"
                       FontSize="48"
                       FontWeight="SemiBold"
                       TextAlignment="Right"
                       Margin="10,0,15,0" />
        </Grid>

        <!-- Number Format Selection Area -->
        <Grid Grid.Row="1" Background="#202020" Margin="3,0,3,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <Style x:Key="NumberFormatButtonStyle" TargetType="RadioButton">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Margin" Value="1" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="RadioButton">
                                <Border x:Name="buttonBorder" CornerRadius="4"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#2D2D2D" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#292929" />
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#2D2D2D" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>

            <!-- Number format selection -->
            <RadioButton Grid.Row="0" Grid.Column="0" Content="HEX" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="NumberFormat" IsChecked="True" />
            <RadioButton Grid.Row="0" Grid.Column="1" Content="DEC" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="NumberFormat" />
            <RadioButton Grid.Row="0" Grid.Column="2" Content="OCT" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="NumberFormat" />
            <RadioButton Grid.Row="0" Grid.Column="3" Content="BIN" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="NumberFormat" />

            <!-- Bit length selection -->
            <RadioButton Grid.Row="1" Grid.Column="0" Content="QWORD" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="BitLength" IsChecked="True" />
            <RadioButton Grid.Row="1" Grid.Column="1" Content="DWORD" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="BitLength" />
            <RadioButton Grid.Row="1" Grid.Column="2" Content="WORD" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="BitLength" />
            <RadioButton Grid.Row="1" Grid.Column="3" Content="BYTE" Style="{StaticResource NumberFormatButtonStyle}"
                        GroupName="BitLength" />
        </Grid>

        <!-- Buttons Area -->
        <Grid Grid.Row="2" Margin="3,0,3,3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.Resources>
                <!-- Reusing styles from Standard Calculator -->
                <Style x:Key="ButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="#3B3B3B" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontSize" Value="18" />
                    <Setter Property="FontWeight" Value="Regular" />
                    <Setter Property="Margin" Value="1.5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="buttonBorder" CornerRadius="5"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#323232" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#FF292929" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style x:Key="SpecialButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="#323232" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontSize" Value="14.5" />
                    <Setter Property="FontWeight" Value="Regular" />
                    <Setter Property="Margin" Value="1.5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="buttonBorder" CornerRadius="5"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#3B3B3B" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#FF292929" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style x:Key="MathOperationButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="#323232" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontSize" Value="28" />
                    <Setter Property="FontWeight" Value="Light" />
                    <Setter Property="Margin" Value="1.5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="buttonBorder" CornerRadius="5"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#3B3B3B" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#FF292929" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style x:Key="OperationButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="#4CC2FF" />
                    <Setter Property="Foreground" Value="Black" />
                    <Setter Property="FontSize" Value="30" />
                    <Setter Property="FontWeight" Value="light" />
                    <Setter Property="Margin" Value="1.5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="buttonBorder" CornerRadius="5"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#47B1E8" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="buttonBorder" Property="Background" Value="#42A1D2" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style x:Key="DisabledButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="#323232" />
                    <Setter Property="Foreground" Value="#666666" />
                    <Setter Property="FontSize" Value="14.5" />
                    <Setter Property="FontWeight" Value="Regular" />
                    <Setter Property="Margin" Value="1.5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="IsEnabled" Value="False" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="buttonBorder" CornerRadius="5"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>

            <!-- Row 0 - Special Functions -->
            <Button Grid.Row="0" Grid.Column="0" Content="&lt;&lt;" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="0" Grid.Column="1" Content="CE" Style="{StaticResource SpecialButtonStyle}"
                    Command="{Binding ClearEntryCommand}" />
            <Button Grid.Row="0" Grid.Column="2" Content="C" Style="{StaticResource SpecialButtonStyle}"
                    Command="{Binding ClearCommand}" />
            <Button Grid.Row="0" Grid.Column="3" Content="⌫" Style="{StaticResource SpecialButtonStyle}"
                    Command="{Binding BackspaceCommand}" />

            <!-- Row 1 - HEX buttons/OR -->
            <Button Grid.Row="1" Grid.Column="0" Content="A" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="1" Grid.Column="1" Content="B" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="1" Grid.Column="2" Content="C" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="1" Grid.Column="3" Content="OR" Style="{StaticResource MathOperationButtonStyle}" />

            <!-- Row 2 - More HEX buttons/AND -->
            <Button Grid.Row="2" Grid.Column="0" Content="D" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="2" Grid.Column="1" Content="E" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="2" Grid.Column="2" Content="F" Style="{StaticResource SpecialButtonStyle}" />
            <Button Grid.Row="2" Grid.Column="3" Content="AND" Style="{StaticResource MathOperationButtonStyle}" />

            <!-- Row 3 - 7,8,9,shifts -->
            <Button Grid.Row="3" Grid.Column="0" Content="7" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="7" />
            <Button Grid.Row="3" Grid.Column="1" Content="8" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="8" />
            <Button Grid.Row="3" Grid.Column="2" Content="9" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="9" />
            <Button Grid.Row="3" Grid.Column="3" Content="XOR" Style="{StaticResource MathOperationButtonStyle}" />

            <!-- Row 4 - 4,5,6,NOT -->
            <Button Grid.Row="4" Grid.Column="0" Content="4" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="4" />
            <Button Grid.Row="4" Grid.Column="1" Content="5" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="5" />
            <Button Grid.Row="4" Grid.Column="2" Content="6" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="6" />
            <Button Grid.Row="4" Grid.Column="3" Content="NOT" Style="{StaticResource MathOperationButtonStyle}" />

            <!-- Row 5 - 1,2,3,= -->
            <Button Grid.Row="5" Grid.Column="0" Content="1" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="1" />
            <Button Grid.Row="5" Grid.Column="1" Content="2" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="2" />
            <Button Grid.Row="5" Grid.Column="2" Content="3" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="3" />
            <Button Grid.Row="5" Grid.Column="3" Content="=" Style="{StaticResource OperationButtonStyle}"
                    Command="{Binding EqualsCommand}" />

            <!-- Row 6 - +/-,0,.,= -->
            <Button Grid.Row="6" Grid.Column="0" Content="±" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NegateCommand}" />
            <Button Grid.Row="6" Grid.Column="1" Content="0" Style="{StaticResource ButtonStyle}"
                    Command="{Binding NumberCommand}" CommandParameter="0" />
            <Button Grid.Row="6" Grid.Column="2" Content="." Style="{StaticResource DisabledButtonStyle}" />
            <Button Grid.Row="6" Grid.Column="3" Content="MOD" Style="{StaticResource MathOperationButtonStyle}" />
        </Grid>
    </Grid>
</UserControl>